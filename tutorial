#!/usr/bin/env python3
"""paw interactive tutorial. Run from the paw repo root: ./tutorial"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

try:
    import curses  # noqa: F401
except ImportError:
    print("Error: Python curses module not available.")
    print()
    print("Fixes:")
    print("  macOS (Homebrew): brew reinstall python@3.12")
    print("  Linux (Debian):   sudo apt install python3-curses")
    print("  Windows:          pip install windows-curses")
    sys.exit(1)

from tutorial_engine.engine import main

if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        pass
