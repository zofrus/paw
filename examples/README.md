# paw examples

## buggy-app

A deliberately buggy Python app for practicing with paw review agents. Has intentional security holes, latent bugs, performance issues, and missing tests.

```
> Use the security-reviewer agent on examples/buggy-app/
```

Or during Tech Forge training:

```
paw check
```

See `buggy-app/README.md` for the full list of issues (spoiler-tagged).
